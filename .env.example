# Google Cloud Text-to-Speech Credentials
# Option 1 (recommended for production/Railway): JSON string of service account credentials
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Option 2 (for local development): Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json

# Cache directory for TTS audio and word breakdowns
# - Local development: defaults to .phonics-cache in project root
# - Railway: Create a volume, mount it (e.g., /data/phonics-cache), and set this variable
# PHONICS_CACHE_DIR=/data/phonics-cache

# Optional: Max TTS audio entries to keep in memory (defaults to 128)
# PHONICS_TTS_MEM_CACHE_ENTRIES=128
